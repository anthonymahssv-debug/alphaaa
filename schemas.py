# Pydantic schemas can be expanded here. The current API intentionally preserves
# the existing intelligence-rich JSON structure from data.json instead of
# flattening listings into a weak generic schema.
